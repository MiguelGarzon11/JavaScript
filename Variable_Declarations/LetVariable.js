// let es recomendado para declarar variables que el valor si puede cambiar

let edad = 17;
edad = 18; // Se puede cambiar

console.log(edad); // 18

// Características

// Se puede re-asignar, pero no re-declarar dentro del mismo scope.
// Tiene alcance de bloque (if, for, etc.)
// Ideal para cuando el valor puede cambiar.
