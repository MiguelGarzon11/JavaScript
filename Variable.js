var nombre = "Miguel";
nombre = "Ángel"; // Se pude cambiar

console.log(nombre);

// Características:

//Se puede re-declarar y re-asignar.
//No respeta el bloque(solo el scope de función o global).
//Se usa poco hoy en día para problemas de hoisting y scope confuso.
